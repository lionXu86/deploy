- hosts: slaves
  remote_user: root
  vars:
    source_dir: "/tmp/test1"
    target_dir: "/tmp/test2"

  tasks:
   - name: update
     synchronize: src={{source_dir}} dest={{target_dir}} delete=yes







